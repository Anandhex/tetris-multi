behaviors:
  TetrisAgent:
    trainer_type: ppo
    hyperparameters:
      batch_size: 256
      buffer_size: 100000
      learning_rate: 0.0003
      beta: 0.005
      epsilon: 0.2
      lambd: 0.98
      num_epoch: 3
      learning_rate_schedule: linear
    network_settings:
      normalize: true
      hidden_units: 512
      num_layers: 4
      vis_encode_type: simple
      memory: null
    reward_signals:
      extrinsic:
        gamma: 0.99
        strength: 1.0
      curiosity:
        gamma: 0.99
        strength: 0.3
        encoding_size: 256
        learning_rate: 0.001
    behavioral_cloning: null
    framework: pytorch
    keep_checkpoints: 5
    max_steps: 10000000
    time_horizon: 512
    summary_freq: 10000
    threaded: true

# Add this curriculum section to your existing file
environment_parameters:
  tetromino_types:
    curriculum:
      - name: "brain_dead"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 100
          threshold: 2.0
        value: 1
      - name: "basic_placement"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 100
          threshold: 5.0
        value: 2
      - name: "intermediate_shapes"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 150
          threshold: 15.0
        value: 4
      - name: "advanced_tetris"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 200
          threshold: 30.0
        value: 7

  board_height:
    curriculum:
      - name: "brain_dead"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 100
          threshold: 2.0
        value: 8
      - name: "basic_placement"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 150
          threshold: 10.0
        value: 10
      - name: "intermediate_shapes"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 150
          threshold: 15.0
        value: 15
      - name: "advanced_tetris"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 200
          threshold: 30.0
        value: 20

  hole_penalty_weight:
    curriculum:
      - name: "brain_dead"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 100
          threshold: 2.0
        value: 0.1
      - name: "basic_placement"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 100
          threshold: 5.0
        value: 0.1
      - name: "intermediate_shapes"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 150
          threshold: 15.0
        value: 0.3
      - name: "advanced_tetris"
        completion_criteria:
          measure: "reward"
          behavior: "TetrisAgent"
          signal_smoothing: true
          min_lesson_length: 200
          threshold: 30.0
        value: 0.5
